# Use environment-provided Railway/Postgres settings in deployment.
# Local development can still be configured manually by uncommenting the localhost block below.

spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=${PORT:8080}

# Local development example:
# spring.datasource.url=jdbc:postgresql://localhost:5432/justjavawebsite
# spring.datasource.username=postgres
# spring.datasource.password=alaopost
# spring.jpa.show-sql=true
# server.port=8088
